# Kopieer naar secrets.toml (niet committen) en vul je eigen waarden in.
# Alleen nodig voor CLI-export naar InfluxDB (monte_carlo_vermogen.py).
INFLUX_TOKEN = "jouw-influx-token"
INFLUX_URL = "http://localhost:8086"
INFLUX_ORG = "jouw-org-id"
INFLUX_BUCKET = "monte_carlo"
